import { CSSProperties } from "react";

export default function CarouselCell({style}:  {style: CSSProperties}) {

    return (
        <div className={`absolute border mx-auto`} style={style}>
            
        </div>
    )
}